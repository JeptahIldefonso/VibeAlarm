This project now builds VibeAlarmInstaller.exe with a generated self-contained .NET installer.
IExpress was intentionally bypassed because it returned success without producing an EXE on this machine.
